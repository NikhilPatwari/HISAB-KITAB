# Where the API lives. Leave unset in development — vite.config.ts proxies /api
# to the Spring Boot server on port 8080.
# VITE_API_BASE_URL=https://api.example.com/api

# Change if the backend runs somewhere other than localhost:8080.
# VITE_API_TARGET=http://localhost:8080
